/*
 * To change this template, choose Tools | Templates
 * and open the template in the editor.
 */
package com.variance.vreuter.marketfeed;

import com.variance.vreuter.marketfeed.property.VReuterProperty;
import java.io.File;
import org.junit.After;
import org.junit.Before;
import org.junit.Test;

/**
 *
 * @author marembo
 */
public class XmlFileResourceManagerTest {

  public XmlFileResourceManagerTest() {
  }

  @Before
  public void setUp() {
  }

  @After
  public void tearDown() {
  }

  @Test
  public void testLoadResource() {
//    String path = File.separator + "reuters" + File.separator + "vreuter.xml";
//    XmlFileResourceManager<VReuterProperty> manager = new XmlFileResourceManager<VReuterProperty>();
//    VReuterProperty property = manager.load(path, new VReuterProperty());
//    manager.persist(property, path);
  }
}